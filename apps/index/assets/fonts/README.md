# Geist Mono fonts

Self-hosted WOFF2 files for Windex. The Windex CSP allows fonts from `self`, so font files are copied into the app root rather than loaded from an external origin.

Files:

- `geist-mono-latin.woff2`
- `geist-mono-latin-ext.woff2`
